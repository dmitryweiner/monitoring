# План: локальное распознавание человека в кадре

Статус: оценка, 25 сентября 2026. Не реализовано.

## Цель

Добавить к правилу режима повышенного внимания второе условие:

| Условие | Режим |
| --- | --- |
| изменение > 6 % | включается (действует сейчас) |
| в кадре человек **и** изменение > 3 % | включается (новое) |
| иначе | кадр «спокойный»; два спокойных подряд — обычный режим |

Человек, который почти не двигается (сидит за столом, лежит), меняет кадр на 3–6 %
и сейчас режим не включает. Снижать общий порог до 3 % нельзя: в полосе 3–6 %
почти только рассвет.

## Что показывают данные

Снимки 23–25 сентября (307 штук, 306 пар), сравнение как в monitoring/motion.py:

| Порог | Срабатываний за двое суток |
| --- | --- |
| 10 % | 48 |
| 6 % | 73 |
| 3 % | 111 |

- Пары в полосе 3–10 % в основном идут с 01:00 до 03:15 UTC. Это рассвет:
  солнечные пятна сдвигают структуру кадра, и нормировка яркости этого
  не убирает.
- Есть и пары с человеком в кадре. 23 сентября в 09:40 человек лежит на диване
  (4.4 %), в 09:50 всё ещё лежит (7.2 %), в 10:00 стоит посреди комнаты (8.9 %),
  в 10:10 стоит в дверях (6.4 %). Порог 10 % пропустил всё это,
  6 % ловит три кадра из четырёх, лежащего человека в 09:40 не ловит ни один.
- Распознавание человека отделяет второй случай от первого. Именно поэтому
  3 % имеет смысл только вместе с ним.

## Железо

- Orange Pi 4 Pro: 2× Cortex-A76 до 2.0 ГГц и 6× Cortex-A55 до 1.8 ГГц,
  5.8 ГБ RAM, из них свободно около 4 ГБ.
- Загружен драйвер NPU Vivante `vipcore`. Модель для него нужно конвертировать
  закрытым тулчейном Allwinner (ACUITY, хост x86) в формат `.nb` и запускать
  через их VIPLite. Это отдельная большая работа. Ниже везде вывод на CPU.
- У камеры нет ИК-подсветки. Ночью кадр чёрный, и распознавать там нечего.

## Модель

Нужен **детектор** с классом «person», а не классификатор ImageNet:
в ImageNet нет класса «человек».

| Вариант | Размер | Лицензия | Замечание |
| --- | --- | --- | --- |
| YOLO11n / YOLOv8n (COCO), вход 320×320 | ~6 МБ | AGPL-3.0 | Лучшая точность среди малых; с GPL-3.0 проекта совместима |
| NanoDet-Plus-m 320 | ~4 МБ | Apache-2.0 | Чуть слабее, лицензия свободнее |
| EfficientDet-Lite0 / SSD MobileNetV2 (TFLite) | 4–5 МБ | Apache-2.0 | Проверенные, но заметно хуже на мелких и частично видимых людях |
| Visual Wake Words (MobileNet, 96×96, человек/нет) | < 1 МБ | Apache-2.0 | Очень быстрая, но точность около 85–90 % — мало |

Рекомендация: **YOLO11n в ONNX, вход 320×320**, класс 0 (person), порог уверенности
подобрать на архиве (стартовое значение 0.4). Человек в комнате крупный, 320
должно хватить. Если AGPL нежелательна — NanoDet-Plus-m.

## Среда выполнения

- **onnxruntime 1.30.0** — есть готовая сборка `cp313 manylinux_2_28_aarch64`.
  Нужны также numpy и Pillow (или декодирование через ffmpeg, как в motion.py).
- Агент сейчас не зависит от сторонних пакетов, служба ограничена `MemoryMax=192M`.
  Поэтому распознавание стоит вынести в **отдельный процесс в своём venv**:
  `/opt/monitoring/.venv-cv/bin/python -m monitoring.vision` получает JPEG на stdin
  и печатает JSON `{"person": 0.87}`. Агент вызывает его через subprocess
  с таймаутом, как ffmpeg.
- Процесс на каждый кадр: загрузка модели займёт около 0.3–0.5 с, зато память
  освобождается сразу. При одном кадре в 1–10 минут это приемлемо.
  Если окажется медленно — долгоживущий процесс с общением через stdin/stdout.
- Лимит памяти задаётся отдельно: процесс запускается как часть службы и входит
  в её cgroup. Значит, `MemoryMax` нужно поднять до ~320M, либо запускать
  распознавание отдельной службой (socket-activated unit).

## Оценка нагрузки (проверить замером)

- YOLO11n при 320×320 — около 1.6–2.2 GFLOPs на кадр.
- На двух ядрах A76 в onnxruntime fp32 ожидаю **100–300 мс** на кадр.
  На A55 в 3–4 раза дольше, поэтому процесс стоит привязать к ядрам A76
  (`taskset -c 6,7`: по `lscpu -e` ядра 6 и 7 — A76, 2.0 ГГц).
- Пик памяти около 100–150 МБ.
- Нагрузка: не больше 1440 кадров в сутки (раз в минуту). Суммарно меньше
  5 минут CPU в сутки.
- Распознавание идёт в том же слоте, что и снимок (+30 с), то есть в стороне
  от чтения DHT11.

## Логика в агенте

1. Снимок → эскиз → `changed_fraction` (как сейчас).
2. Если кадр тёмный (σ < 4) — распознавание пропускается.
3. Иначе, если изменение > 3 % (при меньшем изменении результат ничего не решает),
   вызвать `vision` и получить `person_score`.
4. `Attention.observe(fraction, person)`: изменение > 6 %, **или**
   `person` и изменение > 3 % — значимый кадр.
5. В `values` фото добавить `person_score` (0–1), когда распознавание выполнялось.
   Это поле пригодится веб-клиенту и для подбора порога.
6. Ошибка или таймаут распознавания не должны мешать снимку. Кадр оценивается
   только по 6 %, в журнал пишется предупреждение (без изображения).

Настройки в `[attention]`: `person_change_threshold = 0.03`,
`person_score = 0.4`, `vision_command = [...]`, `vision_timeout_seconds = 10`.

## Проверка перед включением

1. Разметить архивные снимки: в кадре человек или нет. Сейчас их около 300,
   с режимом внимания прибавляется. Разметку сделать один раз вручную,
   ~1 час.
2. Прогнать модель по архиву и подобрать порог уверенности.
   Особое внимание ложным срабатываниям: в кадре висят джинсы и одежда
   на сушилке, стоят стул и мягкие игрушки. Малые детекторы при низком пороге
   принимают такое за человека.
3. Замерить на плате время, память и температуру при серии из 60 кадров подряд.
4. Сравнить на архиве: сколько срабатываний добавит правило «человек и > 3 %»
   сверх «> 6 %», и сколько из них ложные.

## Приватность

Всё выполняется локально. Облако получает только число `person_score`,
изображения, как и сейчас, уходят только в закрытый R2.

## Объём работы

| Шаг | Оценка |
| --- | --- |
| venv, модель, `monitoring/vision.py` | 2–3 ч |
| Замеры на плате | 1 ч |
| Разметка архива и подбор порога | 2 ч |
| Интеграция в агент, настройки, тесты, документация, установка | 3–4 ч |
| **Итого** | **около дня** |

## Открытые вопросы

- Допустима ли AGPL-3.0 модели (YOLO), или брать Apache-2.0 (NanoDet)?
- Нужен ли `person_score` в веб-клиенте, или это внутреннее поле?
- Стоит ли когда-нибудь браться за NPU? На CPU нагрузка и так мала.
  NPU имеет смысл, только если распознавание понадобится на каждом кадре
  видеопотока, а не на снимках раз в минуту.
